# resume-fit-scorer
An end-to-end machine learning application that evaluates how well a resume matches a job description. The system uses NLP techniques for text preprocessing and TF-IDF with cosine similarity to generate a match score, along with skill gap analysis to identify missing and overlapping skills. Built with an interactive Streamlit interface.
